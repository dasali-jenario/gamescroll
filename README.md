# Gamescroll

Vertical feed of tiny HTML games. Swipe to browse, tap Play to engage, Done to return.

```bash
npm install
npm run dev
```

Open the local URL, then swipe (or use ↑ / ↓ / J / K). Esc exits play.
